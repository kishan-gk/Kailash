
  # Kailash

  This is a code bundle for Kailash. The original project is available at https://www.figma.com/design/dMYmhhwc6TafRBNdqLnon9/Kailash.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  "# Kailash" 
